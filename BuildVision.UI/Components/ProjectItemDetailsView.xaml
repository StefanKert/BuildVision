<UserControl x:Class="BuildVision.UI.ProjectItemDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:ui="clr-namespace:BuildVision.UI" 
             mc:Ignorable="d"
             Name="Control">
    <Grid DataContext="{Binding ElementName=Control, Path=ProjectItem}">
        <ui:ErrorsGrid x:Name="ErrorsGrid" DataContext="{Binding  ElementName=Control, Path=ProjectItem.ErrorsBox}" />
    </Grid>
</UserControl>
